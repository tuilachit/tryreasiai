# Icon source files

Drop the master Reasi app icon here as `master.png` (or `master.svg` if vector).

The build pipeline will generate the platform-specific sizes (192, 512, 180, etc.) into `public/` from the master file.

Recommended master size: 1024×1024 PNG, square, full color, no transparency around the edges.

